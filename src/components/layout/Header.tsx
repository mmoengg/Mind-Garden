import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="w-full h-20 flex items-center justify-between px-7 z-50">
            <div className="flex  items-center gap-4"></div>
        </header>
    );
};

export default Header;
